#spring.datasource.url=jdbc:postgresql://localhost:5432/shipment_db
#spring.datasource.username=postgres
#spring.datasource.password=manoj123!@#
#
#spring.jpa.hibernate.ddl-auto=update
#spring.jpa.properties.hibernate.default_schema=deliveryspring.application.name=delivery-service
server:
  port: 8082

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/ShiptrackPro
    username: postgres
    password: manoj123!@#
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect


eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
#  flyway:
#    enabled: true
#    locations: classpath:db/migration